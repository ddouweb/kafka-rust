[package]
name = "protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bytes = "1.0"
tokio = { version = "1.0", features = ["full"] }
bincode = "1.3"
thiserror = "1.0"

