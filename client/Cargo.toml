[package]
name = "client"
version = "0.1.0"
edition = "2021"

[dependencies]
# 添加必要的依赖
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
network = { path = "../network" }
protocol = { path = "../protocol" }
